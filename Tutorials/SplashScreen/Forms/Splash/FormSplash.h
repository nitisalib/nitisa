#pragma once

// Include splash form prototype generated by Form Builder
#include "IFormSplash.h"

namespace nitisa
{
	namespace app
	{
		// Declare splash form class
		class CFormSplash :public IFormSplash
		{
		protected:
			// Form close event processing method
			void FormSplash_OnClose(IForm *sender, CloseAction &action) override;
		public:
			// Constructor
			CFormSplash();

			// Method for updating progress
			void setProgress(const float progress);
		};

		// Global variable for splash form
		extern CFormSplash *FormSplash;
	}
}